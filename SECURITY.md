# Security

RemoteX stores saved credentials encrypted with an Android Keystore-backed AES-GCM key.
Do not report real passwords, private keys, or server addresses in public issues.

VNC Authentication is cryptographically weak and does not encrypt session traffic. Use VNC only on a trusted LAN or trusted VPN.
